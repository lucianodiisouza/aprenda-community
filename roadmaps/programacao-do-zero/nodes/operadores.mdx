---
id: operadores
title: Operadores e Expressões
resources:
  - title: "Expressões e operadores (MDN PT-BR)"
    url: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide/Expressions_and_operators
    type: doc
    lang: pt
    free: true
---

**Operadores** são os símbolos que fazem operações com valores. Você já conhece
vários da matemática.

**Aritméticos** - com números:

- `+` soma, `-` subtração, `*` multiplicação, `/` divisão
- `%` resto da divisão (útil para saber se um número é par: `n % 2 === 0`)
- `**` potência (em JavaScript e Python)

**Comparação** - comparam dois valores e devolvem `true` ou `false`:

- `===` igual a (sempre prefira três sinais; `==` faz conversões estranhas)
- `!==` diferente de
- `<`, `>`, `<=`, `>=` menor/maior que

**Lógicos** - combinam condições:

- `&&` (e) - as duas precisam ser verdadeiras
- `||` (ou) - pelo menos uma é verdadeira
- `!` (não) - inverte o valor

Operadores combinados viram **expressões** - toda expressão tem um valor.
`2 + 3` vale `5`; `idade >= 18` vale `true` ou `false`. É o que permite
montar lógica sem precisar escrever várias linhas.

<Checkpoint
  challenge="Imprima o resto da divisão de 17 por 5 (operador %)."
  starterCode={`console.log();`}
  expected="2"
  slug="programacao-do-zero"
  nodeId="operadores"
/>
