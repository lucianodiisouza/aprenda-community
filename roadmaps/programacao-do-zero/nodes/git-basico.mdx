---
id: git-basico
title: "Git: add, commit, push"
resources:
  - title: "Pro Git — livro oficial, traduzido para PT-BR"
    url: https://git-scm.com/book/pt-br/v2
    type: doc
    lang: pt
    free: true
  - title: "GitHub Skills — curso interativo oficial"
    url: https://skills.github.com/
    type: curso
    lang: en
    free: true
---

**Git** é a ferramenta que registra o **histórico** do seu código. Cada vez
que você "salva uma versão" (`commit`), o git guarda um ponto de retorno. É
por isso que todo projeto — dessa trilha, do seu trabalho, de tudo — usa git.

Os quatro comandos que você usa 90% do tempo:

```bash
git init                   # cria um repositório novo na pasta atual
git add .                  # marca todos os arquivos alterados para o próximo commit
git commit -m "mensagem"   # salva um ponto no histórico
git push                   # envia para o GitHub (ou outro servidor)
```