---
id: arrays
title: Listas (Arrays)
resources:
  - title: "Array (MDN PT-BR)"
    url: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/Array
    type: doc
    lang: pt
    free: true
---

Um **array** (ou lista) guarda **vários valores em ordem**, dentro de uma única
variável. É uma das estruturas mais usadas em qualquer programa.

```js
const notas = [8, 9, 7, 10];
notas[0];      // 8 (o primeiro!)
notas.length;  // 4
notas.push(6); // adiciona 6 no final
notas.pop();   // remove o último
notas.includes(9); // true — busca um valor
```
Para **percorrer** todos os itens, o jeito clássico é o `for`. Mas existem
métodos que deixam isso mais legível e poderoso:

```js
const dobrados = notas.map(n => n * 2);
const maioresQue7 = notas.filter(n => n > 7);
const soma = notas.reduce((total, n) => total + n, 0);
```
- `map` — transforma cada item em outro (mesma quantidade de itens).
- `filter` — mantém só os itens que passam no teste.
- `reduce` — combina todos os itens em um único valor.

Map, filter e reduce são a base da **programação funcional** — não precisa
virar especialista agora, mas é bom saber que existem. Eles evitam muitos
`for` complicados.

## Experimente

```javascript
const notas = [8, 9, 7, 10, 6];
console.log("notas:", notas);
console.log("maior:", Math.max(...notas));
console.log("média:", notas.reduce((t, n) => t + n, 0) / notas.length);
console.log("aprovados (>=7):", notas.filter(n => n >= 7));
```